export enum PathId {
  Ingredients,
  Recipes,
  RecipeCollections,
  /** Path to folder where tags are stored */
  Tags,
  Images,
  Thumbnails,
  /** Assets folder inside the app folder */
  AppAssets,
}
