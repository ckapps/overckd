import { CkadButtonDirective } from './ckad-button.directive';

describe('CkadButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CkadButtonDirective();
    expect(directive).toBeTruthy();
  });
});
