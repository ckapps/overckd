// --------------------------------------------------------
// @marblejs/core
// --------------------------------------------------------
// jest.mock('@marblejs/core', () => ({
//   // Everytime createrReader is called, just return the implementation
//   // so that the repositories can just be used
//   createReader: jest.fn(h => h),
// }));
