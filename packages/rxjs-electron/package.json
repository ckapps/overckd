{
  "name": "@ckapp/rxjs-electron",
  "private": "true",
  "description": "RxJS extensions for electron",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "build:watch": "npm run build -- --watch",
    "clean": "rimraf dist",
    "lint": "eslint --ext .ts .",
    "precommit": "lint-staged",
    "test": "jest ."
  },
  "dependencies": {
    "@ckapp/rxjs-snafu": "0.0.23"
  },
  "devDependencies": {
    "electron": "^10.1.5"
  },
  "peerDependencies": {
    "electron": "^10.1.5",
    "rxjs": "~6.6.6"
  }
}
