{
  "name": "@overckd/domain",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "npm run build -- --watch",
    "lint": "eslint --ext .ts .",
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "lint-staged": "^10.5.1",
    "typescript": "~4.0.2"
  }
}
