{
  "name": "@overckd/domain",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "npm run build -- --watch",
    "clean": "rimraf dist",
    "lint": "eslint --ext .ts .",
    "precommit": "lint-staged",
    "test": "jest ."
  },
  "dependencies": {
    "rxjs": "~6.6.0"
  },
  "devDependencies": {
    "lint-staged": "^10.5.1",
    "typescript": "~4.0.2"
  }
}
