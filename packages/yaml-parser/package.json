{
  "name": "@overckd/yaml-parser",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "build:watch": "yarn build -- --watch",
    "clean": "rimraf dist",
    "lint": "eslint --ext .ts .",
    "precommit": "lint-staged",
    "test": "jest ."
  },
  "dependencies": {
    "@overckd/domain": "^0.0.0",
    "fp-ts": "^2.13.1",
    "io-ts": "^2.2.20",
    "js-yaml": "^3.3.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^3.12.5",
    "rxjs": "~7.8.0",
    "typescript": "~4.9.5"
  },
  "peerDependencies": {
    "rxjs": "~7.8.0"
  }
}
